{
    "applicationIdentifier": "{{ .appid }}",
    "filterConfig": {
        "applicationMatchTarget": "URL",
        "applicationMatchType": "CONTAINS",
        "pattern": "{{ .pattern }}"
    },
    "name": "{{ .name }}"
}